#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    newmat = matrix[::]
    for i in range(len(matrix()):
            for x in range(len(matrix[::])):
                newmat[i][x] = matrix[i][x]**2
    return newmat
